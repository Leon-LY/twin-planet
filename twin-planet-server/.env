# 数据库（现有 investlearn-db 容器内新建的 twin_planet 库）
DATABASE_URL=postgresql://twin_planet_user:tp_dev_2026@localhost:5432/twin_planet

# Redis（现有 investlearn-redis 容器，用 DB 2 隔离）
REDIS_URL=redis://localhost:6379/2

# JWT 签名密钥（生产环境必须替换为随机字符串）
JWT_SECRET=change-me-to-a-random-secret-in-production

# 微信小程序
WECHAT_APPID=wxee2ef767a77058db
WECHAT_SECRET=your-wechat-app-secret-here

# 服务端口
PORT=3000
